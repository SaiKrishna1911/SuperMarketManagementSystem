# supermarket-mini-project-DBMS
Supermarket management system

## To install dependencies
```pip install pymysql```

```pip install flask ```

## To run the app
Run the code in terminal in the same location
```python app.py```
